fix: DB path root cause, balance check consistency, coach seed data

- database.js: Add auto-migration from server/academy.db.json to
  server/data/academy.db.json on first startup if the new path is empty.
  This was the root cause of Ahmed balance appearing fixed locally but
  wrong on Railway — the code reads from data/ but we were editing the
  old path.
- Book.jsx: Add balance check so both /book AND /schedule entry points
  show the same session-type picker when balance is sufficient. Previously
  only Schedule had this, making behavior inconsistent.
- server/data/academy.db.json: Corrected Ahmed Saleh to private=4
  group=0, added coach users (Coach Laila, Coach Carlos) and court
  defaults so coach features have data to display.
